import streamlit as st
import pandas as pd

# إعدادات الصفحة لتناسب الموبايل والكمبيوتر
st.set_page_config(page_title="منصة إرشاد المعتمر", layout="wide")

# القائمة الجانبية للتنقل
page = st.sidebar.selectbox("اختر الواجهة", ["لوحة تحكم الشركة", "واجهة المعتمر (تجربة)"])

if page == "لوحة تحكم الشركة":
    st.title("🕋 إدارة الأفواج - المدينة المنورة")
    
    # إحصائيات سريعة
    col1, col2 = st.columns(2)
    col1.metric("الأفواج النشطة", "3")
    col2.metric("حالات التيهان", "0")

    st.subheader("📊 المتابعة الميدانية")
    data = {
        "الاسم": ["أحمد جاسم", "سعاد علي", "محمد حسن"],
        "الحالة": ["في الفندق", "في الحرم", "في الفندق"],
        "رقم الهاتف": ["077xxxx", "078xxxx", "075xxxx"]
    }
    st.table(pd.DataFrame(data))
    
    st.info("💡 هذه اللوحة يراها صاحب الشركة في العراق لمتابعة العمل.")

else:
    st.title("📱 واجهة المعتمر العراقي")
    st.write("أهلاً بك يا زائر بيت الله.. نحن معك")
    
    # أزرار ضخمة للموبايل
    st.markdown("""
        <style>
        div.stButton > button:first-child {
            height: 120px;
            width: 100%;
            font-size: 25px;
            font-weight: bold;
            border-radius: 15px;
            margin-bottom: 20px;
            background-color: #f0f2f6;
        }
        </style>
    """, unsafe_allow_html=True)

    if st.button("📍 اندلني فندقي (الخريطة)"):
        st.success("جاري فتح خرائط جوجل...")
        # هنا يوضع رابط فندق حقيقي لاحقاً
        
    if st.button("⚠️ أنا تائه.. اطلب مساعدة"):
        st.error("تم إرسال موقعك للمندوب.. ابقَ في مكانك")
        st.warning("📞 المندوب العراقي في المدينة سيتصل بك الآن")

    with st.expander("بيانات السكن والطوارئ"):
        st.write("فندق: أنوار المدينة")
        st.write("هاتف المندوب: 05xxxxxxx")
